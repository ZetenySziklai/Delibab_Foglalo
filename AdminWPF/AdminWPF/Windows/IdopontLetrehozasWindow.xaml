<Window x:Class="AdminWPF.Windows.IdopontLetrehozasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Új Időpont Létrehozása"
        Height="230" Width="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Kezdet (pl. 9 = 09:00, 6.5 = 06:30):" Style="{StaticResource LabelStyle}" Margin="0,0,0,2"/>
        <TextBox x:Name="txtKezdet" Grid.Row="1" Style="{StaticResource InputStyle}" Text="9"/>

        <Label Grid.Row="3" Content="Vég (pl. 10 = 10:00, 9.5 = 09:30):" Style="{StaticResource LabelStyle}" Margin="0,0,0,2"/>
        <TextBox x:Name="txtVeg" Grid.Row="4" Style="{StaticResource InputStyle}" Text="10"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnMegse"
                    Content="Mégse"
                    Background="#6C757D"
                    Style="{StaticResource ActionButton}"
                    Width="90"
                    Click="BtnMegse_Click"/>
            <Button x:Name="btnLetrehoz"
                    Content="Létrehozás"
                    Background="#28A745"
                    Style="{StaticResource ActionButton}"
                    Width="110"
                    Margin="10,0,0,0"
                    Click="BtnLetrehoz_Click"/>
        </StackPanel>
    </Grid>
</Window>
