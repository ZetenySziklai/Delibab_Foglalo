<Window x:Class="AdminWPF.Windows.AsztalLetrehozasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Új Asztal Létrehozása"
        Height="230" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Asztal neve / azonosítója:" Style="{StaticResource LabelStyle}" Margin="0,0,0,2"/>
        <TextBox x:Name="txtAsztalNev" Grid.Row="1" Style="{StaticResource InputStyle}" Margin="0,0,0,10"/>

        <Label Grid.Row="2" Content="Férőhelyek száma:" Style="{StaticResource LabelStyle}" Margin="0,0,0,2"/>
        <TextBox x:Name="txtHelyekSzama" Grid.Row="3" Style="{StaticResource InputStyle}" Margin="0,0,0,0" Text="4"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnMegse"
                    Content="Mégse"
                    Background="#6C757D"
                    Style="{StaticResource ActionButton}"
                    Width="90"
                    Click="BtnMegse_Click"/>
            <Button x:Name="btnLetrehoz"
                    Content="Létrehozás"
                    Background="#28A745"
                    Style="{StaticResource ActionButton}"
                    Width="110"
                    Margin="10,0,0,0"
                    Click="BtnLetrehoz_Click"/>
        </StackPanel>
    </Grid>
</Window>
