<Window x:Class="AdminWPF.Windows.FelhasznaloKezelesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Felhasználó kezelése"
        Height="320" Width="430"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="18"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Felhasználó:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
        <ComboBox x:Name="comboFelhasznalok"
                  Grid.Row="1"
                  FontFamily="Segoe UI" FontSize="11"
                  Height="32" VerticalContentAlignment="Center" Padding="6,0"
                  SelectionChanged="ComboFelhasznalok_SelectionChanged"/>

        <Label Grid.Row="3" Content="Művelet:" Style="{StaticResource LabelStyle}" Margin="0,0,0,4"/>
        <ComboBox x:Name="comboMuvelet"
                  Grid.Row="4"
                  FontFamily="Segoe UI" FontSize="11"
                  Height="32" VerticalContentAlignment="Center" Padding="6,0"
                  SelectionChanged="ComboMuvelet_SelectionChanged">
            <ComboBoxItem Content="👑 Admin hozzáférés adása"   Tag="admin_add"/>
            <ComboBoxItem Content="🚫 Admin hozzáférés elvétele" Tag="admin_remove"/>
            <ComboBoxItem Content="🗑 Felhasználó törlése"       Tag="delete"/>
        </ComboBox>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnMegse"
                    Content="Mégse"
                    Background="#6C757D"
                    Style="{StaticResource ActionButton}"
                    Width="100"
                    Click="BtnMegse_Click"/>
            <Button x:Name="btnVegrehajt"
                    Content="Végrehajtás"
                    Background="#007BFF"
                    Style="{StaticResource ActionButton}"
                    Width="140" Margin="10,0,0,0"
                    Click="BtnVegrehajt_Click"/>
        </StackPanel>
    </Grid>
</Window>
