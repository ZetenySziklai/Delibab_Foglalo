<Window x:Class="AdminWPF.Windows.AsztalTorlasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Asztal Törlése"
        Height="230" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Válasszon asztalt a törléshez:" Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
        <ComboBox x:Name="comboAsztalok"
                  Grid.Row="1"
                  FontFamily="Segoe UI" FontSize="11"
                  Height="32" VerticalContentAlignment="Center" Padding="6,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnMegse"
                    Content="Mégse"
                    Background="#6C757D"
                    Style="{StaticResource ActionButton}"
                    Width="100"
                    Click="BtnMegse_Click"/>
            <Button x:Name="btnTorol"
                    Content="🗑 Törlés"
                    Background="#DC3545"
                    Style="{StaticResource ActionButton}"
                    Width="110"
                    Margin="10,0,0,0"
                    Click="BtnTorol_Click"/>
        </StackPanel>
    </Grid>
</Window>
