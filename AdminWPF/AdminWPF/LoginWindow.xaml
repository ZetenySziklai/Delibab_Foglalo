<Window x:Class="AdminWPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Délibáb Étterem - Bejelentkezés"
        Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Fejléc -->
        <Border Background="#2D2D30" Height="60">
            <TextBlock Text="🍽️ Délibáb Étterem - Admin bejelentkezés"
                       Foreground="White"
                       FontFamily="Segoe UI"
                       FontSize="14"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Bejelentkezés panel -->
        <Border Grid.Row="1" Margin="20" Background="White" CornerRadius="5" Padding="20" >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                           Text="Kérlek jelentkezz be az admin felülethez."
                           FontFamily="Segoe UI"
                           FontSize="12"
                           Margin="0,0,0,10"/>

                <TextBlock Grid.Row="1" Grid.Column="0"
                           Text="Felhasználónév:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,5"
                           FontFamily="Segoe UI"
                           FontSize="12"/>
                <TextBox x:Name="txtFelhasznalo"
                         Grid.Row="1" Grid.Column="1"
                         Height="25"
                         FontFamily="Segoe UI"
                         FontSize="12"
                         Margin="0,0,0,5"/>

                <TextBlock Grid.Row="2" Grid.Column="0"
                           Text="Jelszó:"
                           VerticalAlignment="Center"
                           Margin="0,0,10,5"
                           FontFamily="Segoe UI"
                           FontSize="12"/>
                <PasswordBox x:Name="txtJelszo"
                             Grid.Row="2" Grid.Column="1"
                             Height="25"
                             FontFamily="Segoe UI"
                             FontSize="12"
                             Margin="0,0,0,10"/>

                <Button x:Name="btnBejelentkezes"
                        Grid.Row="3" Grid.ColumnSpan="2"
                        Content="Bejelentkezés"
                        Height="30"
                        Margin="0,5,0,0"
                        Background="#007BFF"
                        Foreground="White"
                        FontFamily="Segoe UI"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="BtnBejelentkezes_Click"/>

                <TextBlock x:Name="txtInfo"
                           Grid.Row="4" Grid.ColumnSpan="2"
                           Margin="0,10,0,0"
                           Foreground="#666"
                           FontFamily="Segoe UI"
                           FontSize="10"
                           Text="Teszt felhasználó: admin / admin123"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Top"/>
            </Grid>
        </Border>
    </Grid>
</Window>

